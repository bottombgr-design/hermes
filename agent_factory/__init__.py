"""Hermes Agent Factory (Phase 7 MVP).

CLI-only edge feature for authoring, validating, rendering, testing, and
(fail-closed, fixture-only) deploying declarative ``agent.yaml`` specs into
new, factory-managed *test* profiles. Never a core model tool: nothing in
this package is registered as an agent-callable tool.

See ``docs/design/agent-factory.md`` for the architecture and safety model.
"""
